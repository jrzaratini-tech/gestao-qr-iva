rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /workspaces/{workspaceId}/state/{docId} {
      allow read, write: if request.auth != null
        && workspaceId.matches('^[a-z0-9-]{6,80}$')
        && docId == 'main';
    }
  }
}
